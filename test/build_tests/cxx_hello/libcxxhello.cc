#include "common.h"

std::string Ss(std::string v) {
    return v + "a";
}

std::string say_hello(std::string name) {
    return std::string("Hello, " + name);
}

int add(int x, int y) {
    return x + y;
}
